#include<bits/stdc++.h>
using namespace std ;
void area(vector <double> t)
{
    for(int i=0 ;i<t.size();i++)
    t[i]=t[i]*t[i]*M_PI ;
}

double check(vector <double> cake)
{
      double low=cake[0] , high =cake[cake.size-1] ;
      mid=low+(high-low)
}
int main()
{
    int t ,  f,n ;
    cin>>t ;
    while(t--)
    {
        vector<double> cake(n)
        auto (i:n)
        {
            cin>>cake[i] ;
        }
        
        sort(cake.begin(),cake.end()) ;
      
        double size=check(cake) ;
    }
}